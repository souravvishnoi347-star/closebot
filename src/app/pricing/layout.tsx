import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pricing",
  description: "Affordable pricing plans for Chatmagnet AI — the WhatsApp CRM with AI automation, multi-agent inbox, lead scoring, and visual flow builder. Start your 3-day free trial today.",
};

export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
