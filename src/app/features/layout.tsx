import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Features",
  description: "Explore Chatmagnet AI's powerful features — AI smart replies, visual flow builder, multi-agent inbox, lead scoring, revenue analytics, and role-based access for your WhatsApp sales pipeline.",
};

export default function FeaturesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
