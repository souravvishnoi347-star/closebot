import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Case Studies",
  description: "See how fast-growing companies use Voltchat AI to boost deal closures, reduce lead response time, and improve sales team productivity.",
};

export default function CaseStudiesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
