import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Integrations",
  description: "Connect Voltchat AI with 15+ tools you already use — Zapier, Google Sheets, Shopify, Razorpay, HubSpot, and more. Seamless WhatsApp CRM integrations.",
};

export default function IntegrationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
